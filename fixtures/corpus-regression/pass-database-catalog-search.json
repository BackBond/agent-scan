{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "tools": [{
      "name": "catalog_search",
      "description": "Search the movie database by title, director, actor, or keywords.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Search query: movie title, director, actor, or keywords."
          }
        },
        "required": ["query"]
      }
    }]
  }
}
