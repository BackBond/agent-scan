{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "tools": [{
      "name": "project_data_question",
      "description": "Query the project database using natural language. The server converts the question into SQL before retrieving records.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Natural-language question, not SQL syntax."
          }
        },
        "required": ["query"]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    }]
  }
}
