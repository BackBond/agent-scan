{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "tools": [
      {
        "name": "open_url",
        "description": "Open and retrieve content from a URL.",
        "inputSchema": { "type": "object", "properties": { "url": { "type": "string", "description": "URL to open." } } }
      },
      {
        "name": "deploy_release",
        "description": "Deploy a release to production.",
        "inputSchema": { "type": "object", "properties": { "release_id": { "type": "string" } } }
      }
    ]
  }
}
