{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "tools": [{
      "name": "dataframe_query",
      "description": "Run a read-only SQL SELECT against tables staged on a dataframe canvas.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "sql": {
            "type": "string",
            "description": "SQL SELECT to run against the staged tables."
          }
        },
        "required": ["sql"]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    }]
  }
}
