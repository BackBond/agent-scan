{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "tools": [{
      "name": "explain_shell",
      "description": "Explains what a shell command does. It does not execute anything; output is prose only",
      "inputSchema": {
        "type": "object",
        "properties": {
          "command": {
            "type": "string",
            "description": "The shell command to explain"
          }
        },
        "required": ["command"]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    }]
  }
}
