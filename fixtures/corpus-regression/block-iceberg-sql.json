{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "tools": [{
      "name": "workspace_query",
      "description": "Run a SQL query against the Iceberg tables loaded into a workspace.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "sql": {
            "type": "string",
            "description": "SQL query, for example SELECT * FROM customers LIMIT 10."
          }
        },
        "required": ["sql"]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    }]
  }
}
