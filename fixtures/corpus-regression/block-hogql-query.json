{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "tools": [{
      "name": "analytics_query",
      "description": "Run an arbitrary HogQL (SQL) query against analytics data.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "HogQL query string."
          }
        },
        "required": ["query"]
      }
    }]
  }
}
