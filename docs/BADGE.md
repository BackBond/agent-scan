# Published badge

Badges are available only after a pinned private analyzer produces an eligible envelope and the operator explicitly uses `--publish`.

The public client does not authenticate the analyzer publisher. A badge must
not be described as independently verified merely because the analyzer bytes
matched a caller-supplied SHA-256 pin.

A badge is a point-in-time summary, not a disclosure of the analyzer implementation, certification, or substitute for the signed evidence receipt.
